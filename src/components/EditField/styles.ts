import {StyleSheet} from 'react-native';

export const themes = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row',
        alignItems:'center',
        bottom:50
    },
    button: {
        marginLeft: 150,
    }
})