import {StyleSheet} from 'react-native';

export const themes = StyleSheet.create({
    button: {
        marginStart: 250,
        width: 25,
        height: 25,
        borderRadius: 25/2,
        borderWidth:1,
        borderColor:'black',
        textAlign:'center'
    },
})