import {StyleSheet} from 'react-native';

export const themes = StyleSheet.create({
    button: {
        flex:1,
        width: 25,
        height: 25,
        borderRadius: 25/2,
        borderWidth:1,
        borderColor:'black',
        flexDirection: 'row'
    },
})
