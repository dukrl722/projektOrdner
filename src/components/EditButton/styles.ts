import {StyleSheet} from 'react-native';

export const themes = StyleSheet.create({
    button: {
        backgroundColor: 'black',
        position: 'absolute',
        borderWidth:10,
        borderColor:'black',
        textAlign:'center'
    }
})