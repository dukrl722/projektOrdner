import {StyleSheet} from 'react-native';

export const themes = StyleSheet.create({
    button: {
        borderRadius:50,
        borderWidth:10,
        borderColor:'black',
        textAlign:'center'
    }
})